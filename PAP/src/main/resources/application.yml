spring.application.name: PAP
server.port: 8091
spring:
  datasource:
    url: jdbc:mysql://localhost:8306/PAP?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: csc1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
